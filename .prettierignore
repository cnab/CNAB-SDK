# Generated or vendored
package-lock.json
site/
lib/
dist/
packages/core/src/spec.generated.ts
bindings/java/target/

# Committed test fixtures — byte-exact by definition
packages/core/test/golden/

# Generated by packages/core/test/generate-api-surface.cjs, which writes it with
# JSON.stringify(…, 2). Prettier collapses short arrays onto one line, so the two
# would fight forever; and one member per line is exactly what you want when
# reviewing the diff of a public-API change.
packages/core/test/api-surface.json

# The spec is hand-authored DATA, not code. It uses a deliberate compact flow
# style (`- { ref: codigo_banco, pos: [1, 3] }`) so a record reads as a table of
# positions; prettier expands every entry to multiple lines, which makes review
# of a position change — the thing most likely to corrupt a file — far harder.
*.yml
*.yaml

# Prose. Reflowing paragraphs churns diffs without making anything clearer.
*.md
